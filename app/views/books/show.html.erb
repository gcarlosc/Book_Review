<h1><%= @book.title %></h1>
<p><%= @book.description %></p>
<p><%= @book.author %></p>

<% if user_signed_in? %>
<% if @book.user_id == current_user.id %>
<%= link_to "Edit", edit_book_path %>
<%= link_to "Delete", book_path, method: :delete, data: {confirm: "are u sure?"} %>
<% end %> 
<% end %>